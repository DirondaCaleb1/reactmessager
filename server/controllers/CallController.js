import {
  GetAllCalls,
  SaveCalls,
  UpdateCallFinishing,
} from "../services/CallService.js";

export const SaveCallsController = SaveCalls;

export const UpdateCallFinishingController = UpdateCallFinishing;

export const GetAllCallsController = GetAllCalls;
